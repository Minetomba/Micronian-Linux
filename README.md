# OSF-Linux
**O**ne-**S**-tatic-**F**ile Linux is a minimalist distribution designed to be as small and efficient as possible.

## Installation
Compile with "./compile.sh" and tell the bootloader to call the "osf" binary here.